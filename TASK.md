# TASK.md

## 1. 任务目标

基于以下文档，完成 CodexSwitcher 首页交互原型：

- `DESIGN.md`
- `04-架构与原型文档.md`
- `CodexSwitcherMac/`
- `CodexSwitcherWin/`

本任务文档是给 Codex 直接执行的开发任务说明，尽量减少额外解释成本。

## 2. 本阶段交付范围

第一阶段只做：

- 首页原型
- 可复用 UI 组件
- 基于真实 Codex 登录态或 unknown 空态的渲染
- 干净、轻量的静态交互

本阶段不做：

- 真实后端接入
- 真实账号切换
- 数据库读取
- 深色模式

## 3. 需要维护的文件

共享原型文件：

- `prototype/index.html`
- `prototype/styles.css`
- `prototype/app.js`

未来迁移时建议映射到：

- `src/components/`
- `src/pages/`
- `src/modules/dashboard/`

## 4. 首页必须包含的区块

首页必须按以下顺序出现：

1. 顶部导航
2. 当前活跃账号总览卡
3. 多账号状态总览卡
4. 用量历史图表卡
5. 账号时间轴卡
6. 推荐动作卡

## 5. 必做组件

本阶段需要抽象出这些组件：

- `TopNav`
- `MetricCard`
- `StatusTag`
- `AccountListItem`
- `ChartPanel`
- `TimelineLane`
- `RecommendationCard`

## 6. 真实数据与 unknown 空态要求

需要准备以下真实接口返回与 unknown 空态数据结构：

- 当前活跃账号
- 账号列表
- 最近用量点位
- 时间轴预测数据
- 推荐动作列表

最低要求：

- 至少 3 个账号
- 至少包含：健康、预警、耗尽 三种状态
- 图表至少覆盖 24 小时点位
- 时间轴至少覆盖 6 小时片段

## 7. 交互要求

需要实现以下轻交互：

- 用量图表时间切换：`24h / 3d / 7d`
- 用量维度切换：`5h / 7d`
- 时间轴范围切换：`3h / 6h / 12h`
- segmented control 高亮当前项
- 卡片 hover 反馈
- 账号行 hover 反馈

说明：

本阶段可以先保持图表和时间轴数据静态，只要 UI 状态切换能跑通即可。

## 8. 视觉验收标准

原型达到以下标准才算合格：

- 符合 `DESIGN.md`
- 风格浅色、轻盈、安静
- 第一屏能快速表达当前账号状态
- 在笔记本宽度下结构不乱
- 卡片之间的视觉风格一致
- 图表和时间轴缺少真实数据时必须显示 unknown，不允许补假百分比

## 9. 技术验收标准

- 直接打开 `prototype/index.html` 就能运行
- 第一版不依赖构建工具
- 结构便于未来迁移到 React
- 样式变量清楚、可复用
- JavaScript 只负责真实接口返回、unknown 空态和简单 UI 状态

## 10. 分步开发计划

### 第 1 步

建立原型目录和基础文件：

- `prototype/index.html`
- `prototype/styles.css`
- `prototype/app.js`

### 第 2 步

建立全局设计令牌：

- 颜色
- 间距
- 圆角
- 阴影
- 字体

### 第 3 步

搭建页面骨架：

- 顶部导航
- 页面容器
- 响应式区块布局

### 第 4 步

搭建核心卡片：

- 当前活跃账号卡
- 多账号总览卡
- 推荐动作卡

### 第 5 步

搭建数据展示区：

- 图表区域
- 时间轴区域

### 第 6 步

接入真实接口与 unknown 空态渲染：

- 账号列表
- 图表图例
- 推荐动作列表

### 第 7 步

补轻交互：

- segmented controls
- 当前 tab 高亮
- 选择态切换

### 第 8 步

做细节打磨：

- 空态
- 间距
- hover
- 辅助文案

## 11. 后续任务建议

首页原型完成后，下一步建议继续做：

1. 账号中心原型
2. 接力中心原型
3. 设置页原型
4. 迁移为 `React + Tauri`
5. 保持真实模块接口，不引入 mock 数据
